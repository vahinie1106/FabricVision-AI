model_path: models/flux-kontext
hf_model_id: eramth/flux-kontext-4bit
official_model_id: black-forest-labs/FLUX.1-Kontext-dev
device: auto
precision: bfloat16

# Memory: bnb NF4 + model_cpu_offload is the validated RTX 3050 6GB path.
# Kaggle T4 (~15GB) must NOT inherit this: scripts/run_kaggle.py sets
# FLUX_MODEL_CPU_OFFLOAD=false and the loader auto-offloads only <14GB.
# YAML is not the T4 runtime source of truth. Do NOT enable sequential_cpu_offload
# with bitsandbytes (meta-tensor risk).
enable_model_cpu_offload: true
enable_attention_slicing: false
# CLIP prompts are ~70 tokens; T5 at 512 measured ~360s encode on this GPU.
max_sequence_length: 128
# VAE tiling softens garment edges — keep false unless OOM on larger sizes
enable_vae_tiling: false

# Optimizations (overridable via env)
enable_torch_compile: false
attention_backend: auto  # auto | sdpa | flash | xformers | default
profile: true
# Pre-encode T5 once, cache embeds, then free text encoders before diffusion.
# Measured Candidate A: encode ~48–50s at seq=128; avoids re-paying T5 under offload.
preencode_prompt: true

default_generation_mode: standard

# =============================================================================
# Generation modes — values from Day-17 MEASURED runs on RTX 3050 6GB
# (NF4 + model_cpu_offload + SDPA). DO NOT raise steps without a new benchmark:
# each extra step costs ~120–400s under CPU↔GPU offload thrash.
#
# Measured cold Standard 512×512 / 3 steps ≈ 603s (~10 min)
# Measured Production 512×512 / 4 steps ≈ 12–20 min (thermal variance)
# 12–16 step configs previously pushed totals toward ~19–32 min — rejected.
# 640/768 did NOT fix softness and cost ~2–2.5× runtime — rejected as defaults.
# <2.5 min / 5-min catalog-quality is NOT achievable on this hardware path.
# =============================================================================

# Preview — structure check (usable fabric check; still soft vs Production)
preview:
  height: 384
  width: 384
  num_inference_steps: 4
  guidance_scale: 2.5
  measured_runtime_note: "~7-10 min cold on RTX 3050; override FLUX_PREVIEW_STEPS"

fast_preview:
  height: 384
  width: 384
  num_inference_steps: 3
  guidance_scale: 2.5
  measured_runtime_note: "alias for fastest structure check (3 steps)"

# Standard — low-VRAM / local-dev default (RTX 3050 6GB).
# On Kaggle Tesla T4 (~15GB), GarmentGenerationPipeline + flux_vram_policy
# select Standard 712×8 (pack-aligned internally to a 16px multiple).
# Primary heavy inference target is Kaggle T4 / T4×2 via scripts/run_kaggle.py.
standard:
  height: 512
  width: 512
  num_inference_steps: 3
  guidance_scale: 3.0
  measured_runtime_s_cold: 603.5
  note: "low-VRAM default; high-VRAM auto-upgrade applies on T4"

# Explicit T4 Standard (712×712 / 8 steps / guidance 3.0).
# Apply via flux_vram_policy on ≥14GB; diagnostic 4-step: FLUX_STANDARD_STEPS=4.
# 768×12 remains Production / FLUX_ALLOW_HIGH_RES only.
standard_t4:
  height: 712
  width: 712
  num_inference_steps: 8
  guidance_scale: 3.0
  measured_runtime_note: "Kaggle T4 Standard 712×8 GPU-resident; park transformer before fp32 VAE"

# Production — Kaggle T4×2 locked target: 768×768 / 12 steps / guidance 3.0.
# Override via FLUX_PRODUCTION_RESOLUTION / FLUX_PRODUCTION_STEPS /
# FLUX_PRODUCTION_GUIDANCE only. Do NOT inherit FLUX_GENERATION_RESOLUTION
# or FLUX_STANDARD_STEPS. Do NOT silently demote 768→512.
production:
  height: 768
  width: 768
  num_inference_steps: 12
  guidance_scale: 3.0
  measured_runtime_note: "Kaggle T4×2 Production lock 768×12@3.0; OOM must fail clearly"

high_quality:
  height: 768
  width: 768
  num_inference_steps: 12
  guidance_scale: 3.0
  measured_runtime_note: "alias of production"

# =============================================================================
# QUALITY-FIRST presets for ~16GB+ VRAM GPUs
# Keep LOW_VRAM modes above unchanged for RTX 3050 6GB compatibility.
# These presets do NOT auto-disable NF4; use FLUX_QUANTIZATION=full|nf4.
# =============================================================================

# 15-step quality at 512 (primary step-ladder experiment)
quality_15:
  height: 512
  width: 512
  num_inference_steps: 15
  guidance_scale: 3.0
  measured_runtime_note: "quality ladder; expect multi-minute runtime under offload"

# 20-step quality at 512
quality_20:
  height: 512
  width: 512
  num_inference_steps: 20
  guidance_scale: 3.0
  measured_runtime_note: "quality ladder mid point"

# 30-step quality at 512
quality_30:
  height: 512
  width: 512
  num_inference_steps: 30
  guidance_scale: 3.0
  measured_runtime_note: "quality ladder high steps"

# Higher-resolution quality (square). Prefer over 768x1024 unless portrait
# garment framing is validated for Kontext.
quality_768:
  height: 768
  width: 768
  num_inference_steps: 20
  guidance_scale: 3.0
  measured_runtime_note: "requires ~16GB class VRAM for full precision; NF4+offload may still OOM"
