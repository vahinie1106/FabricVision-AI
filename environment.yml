name: fabricvision-ai
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - torch
      - torchvision
      - transformers
      - bitsandbytes
      - pyyaml
      - pillow
      - pytest
      - psutil
